# 房地产样板主题

这里是第一个验证主题的模型模板，当前不包含任何真实市场结论。

使用顺序：

1. 在 `topic.yaml` 定义研究范围。
2. 在 `factors.yaml` 定义因子边界和评估方法。
3. 在 `causal-graph.yaml` 登记因果边和证据要求。
4. 在 `scenarios.yaml` 定义基准、向好、压力情景。
5. 在 `behavior-tree.yaml` 定义研究运行门禁。
