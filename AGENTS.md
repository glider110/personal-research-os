# Personal Research OS 项目规则

## 项目边界

- 当前范围只到“数据源 → 指标 → 因子”。先做数据蒸馏；因果推演、情景和行为树退出默认流程，旧记录仅保留历史存档。
- 不把投资结论当作事实，不把 NotebookLM 或 Agent 的草稿直接当作正式数据。
- 不在本仓库自动执行交易，不保存账号、Token、Cookie 或其他敏感凭据。

## 数据规则

- `02-sources/` 中的正式来源不可覆盖；来源变更必须创建新的 snapshot。
- `90-system/catalog/`、`90-system/db/schema.sql` 和 `90-system/schemas/` 是 Git 管理的定义资产。
- `90-system/runtime/`、大体积原始文件和临时导出物默认不进入 Git。
- `90-system/history/` 中的运行快照只追加，不修改历史判断。
- 每个指标和候选因子必须能追溯到来源、原文核对记录和结果包版本。

## 工作方式

- 修改数据契约或目录边界时，先更新 `00-guide/`，再修改实现。
- 新增主题使用 `90-system/schemas/distillation-package.schema.json`，不复制旧因果模型和结论。
- 指标到因子须核对来源、时间与口径，去除重复/衍生表达的重复计数，并说明旁证、反证、排除理由和逻辑核验结果。验证不足只保留候选因子。
- 用户偏好简明，不增加无必要的层级、图或流程；日常入口只呈现来源、指标表和因子表。
- 不自动提交 Git；提交由用户明确要求后执行。
- 文档和配置默认使用 UTF-8 与 Markdown/YAML/JSON/SQL。
